import BlogSection from "@/src/features/blog";

export const metadata = {
  title: "Blog — Freebuff",
  description: "Read city stories, local news, and travel tips from Ahmedabad.",
};

export default function BlogPage() {
  return <BlogSection />;
}
